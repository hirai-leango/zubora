# Placeholder images

- `og-image.png` (1200x630) and `logo.png` (512x512) are solid-color placeholders.
- Replace them with real brand artwork before launch (OG image is used for SNS shares, logo is referenced by JSON-LD structured data).
